This folder contains two subfolders containing files for auto. segmenting.
If auto. segmenting is incorporated into SayMore, then move this folder
(i.e. AutoSegmenter) and its two sub folders to the DistFile folder. Then
the files will be included in the installer.